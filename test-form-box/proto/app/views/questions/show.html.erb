<p id="notice"><%= notice %></p>

<p>
  <strong>Qtype:</strong>
  <%= @question.qtype %>
	(<%= @question.enquete_id %>)
</p>

<%= link_to 'Edit', edit_question_path(@question) %> |
<%= link_to 'Back', questions_path %>
