<h1>アンケート一覧</h1>
<%= link_to '新規アンケート作成', new_enquete_path %>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
		<% @enquetes.each do |enquete| %>
      <tr>
        <td><%= enquete.title %></td>
        <td><%= link_to 'Show', enquete %></td>
        <td><%= link_to 'Edit', edit_enquete_path(enquete) %></td>
        <td><%= link_to 'Destroy', enquete, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<!--
<div id="result">現在時刻::<%= Time.now %></div>
<%= link_to '更新', {action: :upanel }, remote: true %>
//-->

